/**
 * solution - check if a string ends with the other.
 * @param str
 * @param ending
 * @returns true if str ends with the ending string
 */

const solution = (str, ending) => {
  return str.endsWith(ending) ? true : false;
};
